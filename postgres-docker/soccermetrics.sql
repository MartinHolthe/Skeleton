CREATE TABLE IF NOT EXISTS accounts (
    id serial PRIMARY KEY,
    player_name text NOT NULL,
    nationality text  NOT NULL,
    "position" text  NOT NULL,
    squad text NOT NULL,
    age text NOT NULL,
    birth_year integer NOT NULL,
    games numeric,
    games_starts numeric,
    minutes numeric,
    goals numeric,
    assists numeric,
    pens_made numeric,
    pens_att numeric,
    cards_yellow numeric,
    cards_red numeric,
    goals_per90 numeric,
    assists_per90 numeric,
    goals_assists_per90 numeric,
    goals_pens_per90 numeric,
    goals_assists_pens_per90 numeric,
    xg numeric,
    npxg numeric,
    xa numeric,
    xg_per90 numeric,
    xa_per90 numeric,
    xg_xa_per90 numeric,
    npxg_per90 numeric,
    npxg_xa_per90 numeric,
    minutes_90s numeric,
    shots_total numeric,
    shots_on_target numeric,
    shots_free_kicks numeric,
    shots_on_target_pct numeric,
    shots_total_per90 numeric,
    shots_on_target_per90 numeric,
    goals_per_shot numeric,
    goals_per_shot_on_target numeric,
    npxg_per_shot numeric,
    xg_net numeric,
    npxg_net numeric,
    passes_completed numeric,
    passes numeric,
    passes_pct numeric,
    passes_total_distance numeric,
    passes_progressive_distance numeric,
    passes_completed_short numeric,
    passes_short numeric,
    passes_pct_short numeric,
    passes_completed_medium numeric,
    passes_medium numeric,
    passes_pct_medium numeric,
    passes_completed_long numeric,
    passes_long numeric,
    passes_pct_long numeric,
    xa_net numeric,
    assisted_shots numeric,
    passes_into_final_third numeric,
    passes_into_penalty_area numeric,
    crosses_into_penalty_area numeric,
    progressive_passes numeric,
    passes_live numeric,
    passes_dead numeric,
    passes_free_kicks numeric,
    through_balls numeric,
    passes_pressure numeric,
    passes_switches numeric,
    crosses numeric,
    corner_kicks numeric,
    corner_kicks_in numeric,
    corner_kicks_out numeric,
    corner_kicks_straight numeric,
    passes_ground numeric,
    passes_low numeric,
    passes_high numeric,
    passes_left_foot numeric,
    passes_right_foot numeric,
    passes_head numeric,
    throw_ins numeric,
    passes_other_body numeric,
    passes_offsides numeric,
    passes_oob numeric,
    passes_intercepted numeric,
    passes_blocked numeric,
    sca numeric,
    sca_per90 numeric,
    sca_passes_live numeric,
    sca_passes_dead numeric,
    sca_dribbles numeric,
    sca_shots numeric,
    sca_fouled numeric,
    sca_defense numeric,
    gca numeric,
    gca_per90 numeric,
    gca_passes_live numeric,
    gca_passes_dead numeric,
    gca_dribbles numeric,
    gca_shots numeric,
    gca_fouled numeric,
    gca_defense numeric,
    tackles numeric,
    tackles_won numeric,
    tackles_def_3rd numeric,
    tackles_mid_3rd numeric,
    tackles_att_3rd numeric,
    dribble_tackles numeric,
    dribbles_vs numeric,
    dribble_tackles_pct numeric,
    dribbled_past numeric,
    pressures numeric,
    pressure_regains numeric,
    pressure_regain_pct numeric,
    pressures_def_3rd numeric,
    pressures_mid_3rd numeric,
    pressures_att_3rd numeric,
    blocks numeric,
    blocked_shots numeric,
    blocked_shots_saves numeric,
    blocked_passes numeric,
    interceptions numeric,
    clearances numeric,
    errors numeric,
    touches numeric,
    touches_def_pen_area numeric,
    touches_def_3rd numeric,
    touches_mid_3rd numeric,
    touches_att_3rd numeric,
    touches_att_pen_area numeric,
    touches_live_ball numeric,
    dribbles_completed numeric,
    dribbles numeric,
    dribbles_completed_pct numeric,
    players_dribbled_past numeric,
    nutmegs numeric,
    carries numeric,
    carry_distance numeric,
    carry_progressive_distance numeric,
    progressive_carries numeric,
    carries_into_final_third numeric,
    carries_into_penalty_area numeric,
    pass_targets numeric,
    passes_received numeric,
    passes_received_pct numeric,
    miscontrols numeric,
    dispossessed numeric,
    cards_yellow_red numeric,
    fouls numeric,
    fouled numeric,
    offsides numeric,
    pens_won numeric,
    pens_conceded numeric,
    own_goals numeric,
    ball_recoveries numeric,
    aerials_won numeric,
    aerials_lost numeric,
    aerials_won_pct numeric
);